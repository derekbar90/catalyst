[{
  "id": "${KETO_RESOURCE_PREFIX}policies:policy:by:role:admin",
  "description": "This is another example policy which works together with the example consumer and resource server app.",
  "subjects": ["${KETO_SUBJECT_PREFIX}admin"],
  "effect": "allow",
  "actions": [
     "${KETO_ACTION_PREFIX}create",
     "${KETO_ACTION_PREFIX}read", 
     "${KETO_ACTION_PREFIX}update", 
     "${KETO_ACTION_PREFIX}delete"
  ],
  "resources": [
      "${KETO_RESOURCE_PREFIX}user.list"
  ]
}]
